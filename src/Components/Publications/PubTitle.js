import React from "react";
import styled from "@emotion/styled/macro";

const Title = styled.div`
  font-size: 2rem;
  text-decoration: underline;
`;

const PubTitle = () => {
  return <Title>Publications</Title>;
};

export default PubTitle;
